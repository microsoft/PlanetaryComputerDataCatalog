import { Text } from "@fluentui/react";
import PanelControl from "../PanelControl";

const MapOptionsControl = () => {
  return (
    <PanelControl label="Map Options" iconName="Settings" top={142}>
      <Text>Hi.</Text>
    </PanelControl>
  );
};

export default MapOptionsControl;
